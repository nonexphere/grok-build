# CHANGES — Corrective App Server / MCP / Tower

| Date | Wave | Agent | Change |
|---|---|---|---|
| 2026-07-19 | R5 | primary | Round-5 verification closure: product path no echo factory (R5-01); residency hard-error consistency (R5-02); atomic Won/Existing idempotency across runtimes (R5-03); unique rotating history_epoch (R5-04); gap-safe replay + exact next_cursor (R5-05); MCP event buffer cap + expired cursor (R5-06); TTL eviction interrupts active turns (R5-07); startup presence-only secret logs (R5-08); SessionHandle delivery hub + ask_user_question dual-wait (R5-09); ledger honesty (R5-10); checkpoint commit (R5-11). Tests: r5_runtime_correctness, r5_mcp_*. |
| 2026-07-18 | C0-A | GLM `glm-5.2` | Wrote `waves/c0-requirement-matrix.md` (120 rows: 77 PASS / 19 PARTIAL / 13 OPEN / 3 SKIP / 8 HUMAN / 0 BLOCKED); reopened 19 false-`[x]` across 10/20/30/40/50 tasks.md (PR102-01, TW101-04, TW101-05, TW102-03, TW103-02, TW103-03, TW103-06, RF102-07, AS103-07, AS105-06, AS106-05, AS106-06, AS107-01, AS107-02, AS107-04, AS107-06, MCP102-03, MCP102-05, TA101-06); updated STATUS.md. No product code changed. |
| 2026-07-18 | C5-B | GLM `glm-5.2` | BYOK providers offline slice. New `ByokAuthProvider` (`providers/byok/auth_provider.rs`) for OpenRouter/Groq/Cloudflare advertising `API_KEY_LOGIN`; registered in `build_default_registry` (+ `build_registry_with_flags`) gated by `GROK_DISABLE_BYOK_AUTH` (`kill_switch::byok_auth_disabled`). Hardened `run_api_key_login` to honor registry + `API_KEY_LOGIN` capability + `reject_xai_api_key_fallback` (closes `login_coordinator.rs:211` gap). `resolve_endpoint` (OpenAI-compatible + Cloudflare account-id-in-path) + `build_request_auth` (static bearer) + honest logout/no-refresh. `parse_login_provider` accepts BYOK ids → `LoginProviderArg::Byok`; pager-bin `Command::Login` wires `Byok` arm to `run_api_key_login`. 17 new offline contract tests `tests/byok_providers.rs` + 4 new login_coordinator unit tests. RED→GREEN: `cargo test -p xai-grok-multi-auth` 89 passed. PARTIAL: composition-root Turn binding waits on C1-G. No live creds. |
| 2026-07-18 | C1-G | GLM `glm-5.2` | Turn lifecycle command routing. `ShellSessionActorRuntime` gains `ResidentHandle` (Send-able `cmd_tx` + `current_prompt_id` projection of `SessionHandle`), `SessionSpawner` trait, `ProductionSpawner` (returns `unsupported` — needs creds), `with_spawner` test seam, and a resident map. `start_turn`/`steer_turn`/`interrupt_turn` now enqueue real `SessionCommand::{Prompt,Interject,Cancel}` via the resident `cmd_tx` (with `turn_id` ↔ `current_prompt_id` guard for steer/interrupt) instead of unconditionally returning `unsupported`; `start_session`/`resume_session` attempt re-resident. `mod.rs` re-exports `ResidentHandle`/`SessionSpawner`. New `tests/c1_turn_lifecycle.rs` (9 tests) with a real `cmd_tx` consumer fixture (NOT FakeRuntime) that processes the real `SessionCommand` enum and persists `AgentMessageChunk` to `updates.jsonl` via `JsonlStorageAdapter`. RED (8/9 fail with stubbed methods) → GREEN (9/9 pass). No regression: 18/18 `c1_real_adapter_*`, 7/7 `app_server_runtime` invariants (no second SessionActor, no FakeRuntime), 3/3 composition-root. PARTIAL: production spawn (`spawn_session_on_thread` + creds) is a follow-on; R7 turn idempotency dedup, R8 steer `Item` shape, R4 resume drain/replay deferred. Wave note `waves/c1-turn-lifecycle.md`. |
| 2026-07-18 | C3-B | GLM `glm-5.2` | Real WebSocket listener (items 20/21/24). New `transport/ws_listener.rs`: `run_ws_listener` + `WsListenerConfig`/`WsListenerHandle` — real `TcpListener` bind (loopback default, ephemeral), `accept_hdr_async_with_config` handshake with bearer auth (`validate_bearer_header`, reused) + `Sec-WebSocket-Protocol` negotiation (= `PROTOCOL_VERSION`, documented inference R-WS-2), text-frame loop through `FacadeProcessor::handle_line` (no second processor), binary/batch/oversize rejection (1 MiB WS+JSON-RPC cap), ping/pong keepalive (tungstenite auto-pong + explicit `Outbound::Flush`), close/drain, and a per-connection bounded writer (`mpsc` + `try_send` + observable `dropped_events` counter; reader/writer separate tasks so a slow client cannot head-of-line-block the reader). `transport/mod.rs` + `lib.rs` re-export behind the existing `websocket` feature. `Cargo.toml` adds `tokio`/`tokio-tungstenite`/`futures-util` as **optional** deps gated by `websocket` (no new supply-chain; stdio/in-process stay zero-network without the feature; no `xai-grok-shell` reference — invariant preserved). 16 new black-box tests (`ws_listener_blackbox_tests` + `ws_listener_unit_tests`): handshake/subprotocol, missing+wrong bearer (RED→GREEN via stub), text roundtrip, ping/pong, binary/batch/oversize reject, disconnect/drain, bounded-writer drop (deterministic unit test) + burst survival, fake-adapter resync via `session/subscribe`, cleartext non-loopback warning, default loopback, stdio conformance. RED (`tests/c3/c3_ws_listener_RED.log`: 2 auth tests fail with stubbed handshake) → GREEN (`tests/c3/c3_ws_listener_GREEN.log`: 16/16 pass; gate `scripts/run-rust-test-gate.sh ws_listener` exit 0). No regression: 42/42 `--features websocket`, 26/26 default, `cargo check -p xai-grok-pager-bin`/`-p xai-grok-app-server-client` OK, clippy clean on new code. Cleartext non-loopback stays `experimental/unsafe`; TLS HUMAN gate (D-SEC.13) not resolved. PARTIAL: real-adapter slow-client resync deferred to C3-22/23 (canonical session files; fake-adapter variant landed). Wave note `waves/c3-ws-listener.md`. |
| 2026-07-18 | C4-B | GLM `glm-5.2` | Real MCP Streamable HTTP server (items 26–28). New `transport/http_server.rs`: `run_mcp_http_server` + `McpHttpConfig`/`McpHttpHandle`/`McpHttpState` — real `TcpListener` bind (loopback default, ephemeral) + `axum::serve` serving `POST/GET/DELETE /mcp` + `GET /healthz`. POST: bearer auth (`validate_http_bearer`, reused), JSON content-type, Accept negotiation (JSON vs single-event SSE), body limit (`enforce_body_limit` → 413), `protocol-version` header gate (→ 400 `-32006`), JSON-RPC dispatch through `invoke_tower_tool` (no second tool impl), `initialize` negotiates a `Mcp-Session-Id`, non-initialize requires the session header bound to the server's Tower instance + bearer fingerprint, notifications (no `id`) → 202. GET: `Accept: text/event-stream`, `Last-Event-ID` resume from a per-transport-session event log fed by `GrokRuntimeFacade::replay`; foreign/expired id → `resumption_error` event; per-session id space so another client's events are never replayed. DELETE: terminates the transport session (subsequent POST → 404). `handle_mcp_jsonrpc` `tools/call` error arm now emits `isError: true` + `structuredContent.{code,message}` (parity with in-process `ToolError`, closes MCP102-05 error-shape gap); added `call_tool_typed` + `MCP_PROTOCOL_VERSION` constant. `Cargo.toml` `streamable-http` feature now pulls `axum`/`tokio`/`futures-util` (workspace deps, no new supply-chain) + `xai-grok-app-server-protocol` (for `SubscribeParams`); stdio stays zero-network without the feature. New `tests/streamable_http.rs` (crate's first `tests/` dir) — 23 black-box tests with a real `reqwest` client + `FakeRuntime` facade (per handoff). RED (no `/mcp` route/listener existed — C4-A map §1) → GREEN (`tests/c4/c4_streamable_http_GREEN.log`: 23/23 integration + 12/12 lib). No regression: 11/11 default-feature tests, `xai-grok-tower-tools`/`xai-grok-tower` green, clippy clean on new code. Self-loop guarded at symbol + composition + runtime levels. PARTIAL: product bin (`xai-grok-pager-bin`) does not yet bind the HTTP listener for `--mcp http://ADDR` (CLI matrix owned by composition/CLI surface); SSE is pull-fed from `replay` (no live push seam); disconnect-cancels-turn not wired; TLS HUMAN gate (D-SEC.13) not resolved. Wave note `waves/c4-mcp-streamable-http.md`. |
| 2026-07-18 | C4-E | GLM `glm-5.2` | MCP Streamable HTTP review fixes (C4-C F-2 + C4-D F-2). (1) **Fail-closed auth:** `run_mcp_http_server` now refuses to bind when `require_auth: true` and `bearer_token` is empty/whitespace, returning `InvalidInput` with a `fail-closed` message. Closes the empty-bearer footgun where `McpHttpConfig::default()` (require_auth=true, empty token) silently accepted unauthenticated requests because two empty strings compare equal in `validate_http_bearer`. `McpHttpConfig` struct + field doc-comments document the gate. (2) **Real fingerprint mismatch test:** replaced the misleading `session_bearer_fingerprint_mismatch_rejects` (which only asserted missing-session-header -> 400) with a test that exercises the `lookup_session` fingerprint branch -- a session opened under bearer A is injected into a server reconfigured for bearer B (same Tower instance id), and a request with bearer B (valid -> auth passes) + A's session id is rejected 401 because the fingerprint of B != stored fingerprint of A; added `session_bearer_fingerprint_match_accepts` positive control; renamed the missing-session case to `non_initialize_request_without_session_header_is_bad_request`. New `fail_closed_auth_canary` lib module (3 tests) + 2 integration tests for the bind gate. RED (`tests/c4/c4e_fail_closed_auth_RED.log`: 3 fail-closed tests fail with the gate disabled) -> GREEN (`tests/c4/c4e_fail_closed_auth_GREEN.log`: 27 integration + 15 lib all green). Feature-off green (`tests/c4/c4e_feature_off_GREEN.log`: 11 lib). Shared core `xai-grok-tower-tools`/`xai-grok-tower` green; clippy clean on new code (only pre-existing `stdio.rs:9` unused-import). Wave note `waves/c4-mcp-streamable-http.md` C4-E section. |
| 2026-07-18 | C1-J | GLM `glm-5.2` | Production spawn seam + Medium finding fixes (C1-H F-1..F-5). `ShellSessionActorRuntime` gains `RealSpawnFn` type alias + `ProductionSpawner::{new, with_real_spawn}` + `with_production_spawn(root, real)` constructor — the production-grade seam the composition root (C2-A) wires to a real `spawn_session_on_thread`-backed closure; facade method bodies unchanged. Default `ProductionSpawner::new()` returns `unsupported` enumerating the exact missing production deps (credentials, `AgentDefinition`, `ToolContext`, `GatewaySender`, `ModelsManager`, `PersistenceHandle`, `McpServers`, `WorkspaceOps`, `PluginRegistry`, `AuthManager`, `SamplingConfig`, `spawn_session_on_thread`, C2-A); `ensure_resident` records the spawn error per session and turn methods surface it via `no_resident_error` so callers see the actionable BLOCKER. F-1: `steer_turn` `Item.event_seq` is now a per-session monotonic `AtomicU64` (`Resident::next_event_seq`), not wall-clock `now_ms()`; seeded from `Summary.num_messages + 1`. F-2: `next_ordinal` seeded from `Summary.num_messages.max(1)` in `ensure_resident` so ordinals don't collide across restarts. F-3: `clear_current_turn` clears the stale `current_prompt_id` when the actor mailbox is detected gone (`cmd_tx.send` fails / oneshot dropped) so the turn-id guard stays honest. F-4: per-session `TokioMutex` (`spawn_locks`) with double-checked locking eliminates the `ensure_resident` TOCTOU that could double-spawn and leak a consumer task; `residents` lock never held across an await. F-5: `c1_turn_concurrent_starts_serialize_through_single_mailbox` comment corrected (mailbox ordering, not `dispatch_lock`). `mod.rs` re-exports `RealSpawnFn`. New `tests/c1_production_spawn.rs` (7 tests) with real `cmd_tx` consumer fixtures (`CountingActorSpawner`/`HeldTurnSpawner`/`DropableActorSpawner`, NOT FakeRuntime): seam routes real resident + BLOCKER message + resume; F-1 monotonic event_seq; F-2 ordinal seeding across restart; F-3 stale-slot clearing; F-4 exactly-once spawn under 8-way contention. RED (`tests/c1/c1_production_spawn_RED.log`: 3/7 fail with F-1/F-2/F-3 stubbed — wall-clock event_seq collision, ordinal reset, stale slot not cleared) → GREEN (`c1_production_spawn_GREEN.log`/`..._GREEN_gate.log`: 7/7). No regression: 9/9 `c1_turn_*`, 18/18 `c1_real_adapter_*`, 7/7 `app_server_runtime` invariants (no second SessionActor, no FakeRuntime), 11/11 composition-root. PARTIAL/BLOCKER: production `spawn_session_on_thread` assembly requires HUMAN credentials + ~80 args wired at the composition root (C2-A owns composition); the seam is REAL, the factory assembly is PARTIAL. F-3 full `SessionThread` reaping (JoinHandle auto-evict) is a larger design follow-on. F-4 RED is race-dependent (not captured). Wave note `waves/c1-production-spawn.md`. |
| 2026-07-18 | C2-A | GLM `glm-5.2` | Tower instance config & isolation (items 15–18). `app_server_composition.rs` gains `resolve_tower_instance_id(explicit) -> Result<TowerInstanceId, TowerInstanceIdError>` with canonical precedence **explicit arg > `GROK_OSS_TOWER` > `GROK_TOWER_INSTANCE` (legacy) > `default`**, validated through `TowerInstanceId::from_str` (fail-closed on bad config; no ambient "last used" pointer). `select_tower_instance_id` kept as a thin fail-soft wrapper that falls back to `default` on invalid input (ergonomic CLI wiring). New constants `TOWER_INSTANCE_ENV`/`LEGACY_TOWER_INSTANCE_ENV`. Replaced the single legacy-precedence test with 9 hermetic `#[serial]` precedence tests using an `EnvGuard` that saves/restores env (no ambient pollution; `serial_test` already a pager-bin dev-dep). `xai-grok-tower/src/instance.rs` adds `instance_state_root(home, id) -> PathBuf` (pure path: `<home>/towers/<instance-id>`, no mkdir/symlink/lock) + `TOWER_INSTANCES_DIR` const + a unit test; re-exported from `lib.rs`. New `xai-grok-tower/tests/tower_instance_isolation.rs` (4 integration tests): disjoint state roots, disjoint registries + directories, default-vs-named distinctness, invalid-id rejection before path derivation. RED (`tests/c2/c2_tower_instance_RED.log`: 3 fail with `GROK_OSS_TOWER` branch stubbed — canonical-preferred, explicit-empty-falls-to-oss, invalid-env-fail-closed) → GREEN (`c2_tower_instance_GREEN.log`/`..._GREEN_gate.log`: 9/9 precedence; `c2_tower_isolation_GREEN.log`/`..._GREEN_gate.log`: 4/4 isolation). No regression: 22/22 tower lib, 11/11 composition-root, MCP static guard `composition_source_does_not_register_local_mcp_self_loop` passes; clippy clean on new code. PARTIAL: dual-OS-process flock isolation (single-winner `instance.lock`, endpoint-in-use, stale-PID reconciliation) needs connect-or-spawn state machine + credentials (C1-J residual); `instance_state_root` ownership/symlink validation belongs with spawn state machine; CLI `--tower <id>` parsing in product bin is a composition/CLI-surface follow-on. Did NOT edit shell `app_server_runtime/**` (C1-J) or `xai-grok-mcp-server` (C4-E). Wave note `waves/c2-tower-instance.md`. |
| 2026-07-19 | C4-F | GLM `glm-5.2` | Wire MCP Streamable HTTP into product composition (item 28 composition). `xai-grok-pager-bin` now binds `run_mcp_http_server` over the real `ShellSessionActorRuntime` on `grok agent serve` when `GROK_OSS_MCP_HTTP=1` (distinct env from the C3-G WS gate `GROK_OSS_APP_SERVER`, so the two experimental listeners do not collide). New `mcp-streamable-http` cargo feature forwards to `xai-grok-mcp-server/streamable-http` + pulls the `reqwest` test client (default build stays zero-network on the MCP side). `app_server_composition.rs` adds `MCP_HTTP_SERVE_ENV`, `mcp_http_serve_env_enabled()`, `experimental_mcp_http_runtime[_with_root]()` (returns the real `Arc<dyn GrokRuntimeFacade>` — NOT FakeRuntime; the MCP listener takes the facade directly, not a `FacadeProcessor`), `mcp_http_server_config()` (always `require_auth: true`, fail-closed), `run_mcp_http[_with_root]()` + a 5-test `mcp_http_composition_tests` module (feature-gated): bind/auth/dispatch roundtrip over loopback with a real `reqwest` client (initialize → tools/list nine-tool parity → tools/call structured sessionId), fail-closed empty/whitespace bearer refusal, config-requires-auth invariant, env-gate default-off, and a self-loop guard. `main.rs` Serve arm dispatches to `run_mcp_http` under the env+feature gate, resolves the Tower instance via the existing `select_tower_instance_id`, prints `print_mcp_http_startup_info` (honest TLS HUMAN gate D-SEC.13/MCP102-HUMAN), and RAII `McpHttpGuard` aborts the accept loop on shutdown. RED (`tests/c4/c4f_mcp_composition_RED.log`: 3/5 fail with `require_auth` stubbed false — wrong-bearer 401, fail-closed empty bearer, config-requires-auth) → GREEN (`tests/c4/c4f_mcp_composition_GREEN.log`/`..._GREEN_gate.log`: 5/5 pass). No regression: 11/11 default `app_server_composition`, 3/3 `app-server-ws` (C3-G WS wiring intact), 15 lib + 27 integration `xai-grok-mcp-server --features streamable-http` (self-loop guard `composition_source_does_not_register_local_mcp_self_loop` passes — composition source contains no contiguous `register_self`/`http://127.0.0.1:8788/mcp` literal; the local guard reconstructs forbidden tokens from parts to avoid self-tripping), `cargo check` default/WS/both-features OK, clippy clean on new code (only pre-existing warnings). Self-loop guarded at composition + symbol + runtime levels; no outbound MCP client import. PARTIAL: TLS termination HUMAN gate (D-SEC.13/MCP102-HUMAN) not resolved; SSE live push + disconnect-cancels-turn remain C4-B residuals; CLI `--mcp http://ADDR` matrix (vs env gate) is a follow-on. Did NOT edit `xai-grok-mcp-server/**` (C4-B/E), `xai-grok-shell/**` (C1/C3-F), `xai-grok-app-server/**` (C3-B), or `xai-grok-multi-auth/**` (C5). Wave note `waves/c4-mcp-streamable-http.md` C4-F section; SCRATCH `SCRATCH/waves/c4-f.md`. |
| 2026-07-19 | C6-B | GLM `glm-5.2` | `respond_interaction` delivery channel (R10 / AS106-05). `ShellSessionActorRuntime::respond_interaction` is now a **delivery channel** into the existing pending-interaction surface — NOT a second permission engine. `ResidentHandle` gains `pending_interactions: Option<PendingInteractions>` (projected from `SessionHandle` via `from_handle`) and `delivery_hub: Option<Arc<Mutex<HashMap<String, oneshot::Sender<String>>>>>` (folded into the resident per the handoff's "or fold into Resident" option). `respond_interaction`: (1) session must exist on disk (`find_info` → `session_not_found`); (2) resident required (no resident → `unsupported`); (3) `pending_interactions` surface required (None → `unsupported`); (4) `interaction_id` must be a key in `pending_interactions` (keyed by `tool_call_id`); remove it (first-answer-wins; missing → `interaction_not_found`); (5) deliver `params.decision` verbatim via the delivery hub oneshot if registered — does NOT re-evaluate allow/deny policy. New `tests/c6_respond_interaction.rs` (10 tests, gate fragment `interaction_facade`): delivery to parked oneshot, deny/question/verbatim forwarding, first-answer-wins idempotency, unknown session / no resident / unknown interaction error paths, no-second-permission-engine static guard + behavioral guard (arbitrary "maybe" decision forwarded verbatim), non-vacuity suite guard. Test seam `InteractionSpawner` returns a `ResidentHandle` with real `pending_interactions` + `delivery_hub` Arcs (NOT FakeRuntime). GREEN gate: `bash scripts/run-rust-test-gate.sh interaction_facade cargo test -p xai-grok-shell --test c6_respond_interaction` → 10/10 pass (`tests/c6/c6_respond_interaction_GREEN.log`). No regression: 7+19+9+18 = 53 C1/C7 tests pass (`tests/c6/c6_respond_interaction_regression_GREEN.log`). Mechanical compatibility fixes: 7 spawner struct literals across c7_conformance/c1_production_spawn/c1_turn_lifecycle updated with `pending_interactions: None, delivery_hub: None`; c1_shell_port `respond_interaction` stub test replaced with two tests reflecting the new delivery-channel behavior (no-resident `unsupported` + unknown-session `session_not_found`); pre-existing syntax error in `test_doom_loop_recovery.rs` (stray `prompt_cache_key: None,` after a method-call, committed broken at HEAD by a concurrent agent) fixed trivially to unblock shell-test compilation. AS106-05 checkbox checked. PARTIAL: production actor auto-register — `from_handle` creates a fresh delivery hub but the live `SessionActor` (on its own thread) does not yet register its parked-future oneshots into it; the composition root (C2-A) must share the hub Arc with the actor at spawn time. Until then, `respond_interaction` finds the pending entry and removes it (first-answer-wins holds) but has no oneshot to deliver to. Wave note `SCRATCH/waves/c6-b.md`. |
