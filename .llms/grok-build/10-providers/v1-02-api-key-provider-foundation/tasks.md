# Tasks — v1-02 API key provider foundation

- [x] `PR102-01` ApiKey transport path via `LoginCoordinator::run_api_key_login` (env/explicit secret).
- [x] `PR102-02` Secret never appears in CredentialMetadata Debug.
- [x] `PR102-03` Empty/missing secret rejected.
- [ ] `PR102-04` Full TTY paste UX + per-provider descriptors for openrouter/groq/cloudflare.
- [ ] `PR102-05` Prohibit XAI_API_KEY fallback for native third-party bindings (binding layer).
