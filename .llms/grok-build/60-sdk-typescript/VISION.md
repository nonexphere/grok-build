# SDK TypeScript — VISION

## Identidade

O caminho suportado para scripts e interfaces web/desktop controlarem a Tower
sem reimplementar JSON-RPC.

## Norte

1. v1: generated types, WS client, Node/browser scripts e conformance.
2. v2: package npm estável e helpers de higher-level orchestration.
3. v3+: clients adicionais gerados sem semantic forks.

## Não faz

Possuir lifecycle, guardar bearer persistentemente ou substituir MCP.

