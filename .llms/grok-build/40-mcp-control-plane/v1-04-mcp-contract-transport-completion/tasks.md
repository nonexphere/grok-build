# Tasks — MCP contract and transport completion

- [x] MCP104-01 [F-06] Inline or publish resolvable input/output schemas in tools/list; run `cargo test -p xai-grok-tower-tools`; accept independent JSON Schema compilation for all nine input/output boundaries.
- [x] MCP104-02 [F-02,F-05] Validate tool arguments before lookup/effect and structured output after dispatch; run `cargo test -p xai-grok-tower-tools`; accept missing/unknown/conditional/oversize errors without target lookup.
- [~] MCP104-03 [F-05] Map canonical errors with retryable and operationId in stdio/HTTP; run ./scripts/run-rust-test-gate.sh mcp_error_parity cargo test -p xai-grok-mcp-server -p xai-grok-tower-tools mcp_error_parity; accept identical semantic errors. MCP stdio/HTTP now share public catalog code mapping, structured `code`, `message`, `retryable`, and null `operationId`; a real parity test compares all three fields through both adapters. Operation identity for failed calls and full App Server catalog convergence remain pending.
- [x] MCP104-04 [STDIO] Add grok-oss Tower MCP stdio product launcher with one stdout framing owner; run `scripts/smoke/tower-mcp-stdio.sh`; accept tools/list/call/EOF and zero diagnostic bytes on stdout. Real binary smoke passes.
- [x] MCP104-05 [HTTP] Complete real POST/GET/DELETE and protocol-version/content-negotiation matrix; run `cargo test -p xai-grok-mcp-server --features streamable-http --test streamable_http`; all named lifecycle cases pass.
- [x] MCP104-06 [SSE] Test real actor events, Last-Event-ID, expired cursor, epoch mismatch, rebind, TTL and slow subscriber; run `cargo test -p xai-grok-mcp-server --features streamable-http --test streamable_http`; resumption errors and buffer boundaries pass without leak/gap.
- [~] MCP104-07 [BINDING] Current evidence confirms one transport owns one Tower session/cursor/epoch/event log and rebind is destructive by design. Added `multi-session-binding-contract.md` with required invariants and the unresolved choice of request/header/dedicated subscription API; implementation remains open pending the public contract decision.
- [~] MCP104-08 [INTEROP] HTTP `initialize` + `tools/list` + successful `tools/call` + tool-level argument error pass via independent `rmcp` 2.1; `rmcp::TokioChildProcess` now also consumes the real `grok-oss tower --stdio` binary and passes discovery/call/EOF. Reconnect and full cross-transport matrix remain open.
- [~] MCP104-09 [DEAD] `cargo check -p xai-grok-mcp-server --features streamable-http` is clean for the package; the command still emits an unrelated workspace warning about `xai-grok-pager-bin` registering the same `main.rs` in three binary targets. `process_mcp_stdio_batch` is used by the stdio transport and parity tests, so no removal is justified; full dead-code inventory across product crates remains open.
- [~] MCP104-10 [TD] Added a checked-in NDJSON packet fixture covering `tools/list` and `tools/call`, with an automated order/id/shape assertion and existing real product HTTP/stdio smoke evidence. Full per-mutation RED/GREEN ledger and packet capture across reconnect/error/resume cases remain open.
