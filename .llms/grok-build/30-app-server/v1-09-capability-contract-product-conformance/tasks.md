# Tasks — capability truth and product conformance

- [~] AS109-01 [F-05,F-10] Added typed `method_capability` registry and pre-dispatch fail-closed gate. Product composition now proves disabled `turn/start`, `turn/steer`, and `turn/interrupt` return `runtime_unavailable` before runtime validation/effect, with explicit null `operationId`; unknown methods remain `method_not_found`. Full item/event semantics and all runtime variants remain open.
- [~] AS109-02 [F-05,F-07] Centralized typed `DomainErrorData` projection (`domain_data_for_numeric`) now drives protocol, processor, and WS error envelopes; `operationId: null` is serialized explicitly and covered by protocol plus 41 App Server tests. Full OperationResult/RpcErrorData unification and cross-MCP catalog convergence remain open.
- [~] AS109-03 [F-01,F-08] Product-backed transport legs now pass independently: real Shell-runtime WS bind/auth/session/start/replay-resync, real Shell-runtime MCP HTTP bind/auth/tools/list/call, and real `grok-oss tower --stdio` via rmcp child-process discovery/call/EOF. A single shared normalized success/error/state fixture matrix across all three transports remains open.
- [~] AS109-04 [F-10] Wire all Interaction kinds to parked actor delivery and controller lease; run ./scripts/run-rust-test-gate.sh product_interaction_delivery cargo test -p xai-grok-shell -p xai-grok-app-server product_interaction_delivery; accept reconnect/deny/timeout/closed receiver without double effect. ACP host and Shell facade `interaction/respond` now pass real write-tool roundtrips; product reconnect/lease/timeout/closed-receiver gates remain open.
- [ ] AS109-05 [F-03,F-10] Prove replay/live/reconnect against canonical files and actor events; run ./scripts/run-rust-test-gate.sh product_replay_continuity cargo test -p xai-grok-shell -p xai-grok-app-server product_replay_continuity; accept exact epoch/cursors and no gap/duplicate.
- [ ] AS109-06 [F-10] Complete session fork/resume/archive and turn steer/interrupt product paths or mark capability unavailable; run ./scripts/run-rust-test-gate.sh all_announced_methods cargo test -p xai-grok-app-server all_announced_methods; accept no unsupported for advertised methods.
- [x] AS109-07 [F-07] Protocol schema generation remains byte-for-byte clean: `cargo run -p xai-grok-app-server-protocol --example generate-schema -- --check` passed. Broader SDK/MCP golden consumption remains covered by separate epics.
- [~] AS109-08 [F-08] Added required CI jobs named `fake-conformance` and `product-integration`, both with real Cargo gates and no SKIP relabeling. Branch CI execution and required-check protection remain to be verified in GitHub.
- [~] AS109-09 [DEAD] Read-only inventory created at `runtime-debt-inventory-20260720.md`: 124 raw matches classified into product gaps, human gates, defensive rejects, fixtures and retained compatibility projections; one nested-if Clippy warning was removed without behavior change. App Server/MCP/Tower-tools Clippy is clean; Shell-wide dead-code/clippy pass remains open.
- [ ] AS109-10 [COMPAT] Run native Session versus Codex Thread adapter differential fixtures; accept adapter translates without owning state or changing core semantics.
- [ ] AS109-11 [TD] Capture human product smoke for WS start→turn→items→interaction→interrupt→archive.
