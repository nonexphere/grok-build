# Tasks — nine-tool semantic completion

- [x] TA103-01 [ALL] Generate typed input/output/error models from tower-tools schema and validate before dispatch; run `cargo test -p xai-grok-tower-tools`; all nine input/output boundaries compile independently and argument validation runs before dispatch.
- [~] TA103-02 [LIST] Implement workspace/agent/status/includeArchived/pageSize/cursor filters with stable ordering and foreign cursor errors. Workspace/status/archive filtering, updatedAt-desc/sessionId ordering, page size, and cursor validation are implemented; `agentType` is rejected explicitly until canonical Session metadata includes it.
- [~] TA103-03 [START] Require workspaceRoot/agentType/idempotencyKey; apply model/providerBinding/sandbox overrides through runtime validation and preserve metadata. Required fields, length limits, idempotency, and identifier-only `providerBinding` propagation are implemented; `model`/`sandboxMode` lack fields in the current start contract and canonical metadata remains pending. The real Shell actor prompt bridge now has a bounded deadline and emits `runtime_unavailable` after expiry; provider-backed stdio end-to-end evidence remains open.
- [~] TA103-04 [SEND] Preserve all structured input blocks, enforce 1 MiB total, new_turn/steer_active invariants, active Turn and Interaction errors. Core now validates required send fields, 1..=128 blocks, per-block/aggregate text limits, and preserves text/mention/skill blocks; product actor/interaction gates remain pending.
- [~] TA103-05 [HISTORY] Implement full/last, afterEventSeq, historyEpoch, lastItems, maxBytes, redaction-before-accounting, truncation and next cursor. `full`/`last`, epoch validation, `lastItems`, byte limits, deterministic truncation, and non-zero `afterEventSeq` via canonical `replay` are implemented; full item-delta aggregation and cross-transport cursor conformance remain pending.
- [~] TA103-06 [WAIT] Implement cancellation-safe subscription wait with timeout, real epoch, monotonic nextEventSeq and event/terminal/interaction/timeout/resync reasons. Input cursor/timeout validation, real epoch forwarding, and bounded cooperative polling of canonical replay are implemented; a push subscription/cancellation channel and full wake-reason classification remain pending.
- [~] TA103-07 [INTERRUPT] Shell-backed runtime verifies exact running `turnId` and returns `turn_not_found` for mismatches; matching cancellation and actor-mailbox behavior pass existing C1 lifecycle tests. Fake/tool idempotency and an explicit complete-versus-newer-turn race matrix remain open.
- [~] TA103-08 [RESUME] Shell conformance proves unknown-session rejection, same Session ID on resume, persisted-summary loading, and concurrent resume coverage exists in C1/C7 tests. Full dormant-only admission and explicit concurrent convergence/epoch matrix remain open.
- [~] TA103-09 [ARCHIVE] Shell conformance proves reversible `archived.flag` hide-not-delete behavior and idempotent archive; full busy policy, actor drain/detach, transcript retention, and Tower-tool product matrix remain open.
- [~] TA103-10 [STATUS] Return canonical safe row after ACL, with no resume/side effect or existence leak. Status/list now derives archived/dormant/resident conservatively from observable canonical fields; full actor residency and agent metadata remain pending in product runtime.
- [~] TA103-11 [ERRORS] Emit canonical code/message/retryable/operationId and remove unsupported/invalid_params aliases not in public catalog. All mutator required fields/idempotency keys now fail before runtime lookup, and MCP maps internal aliases to the public catalog; the App Server convergence and failed-operation identity remain pending.
- [~] TA103-12 [PARITY] Semantic core/C6 matrix passes all nine (24 unit + 24 integration); real MCP HTTP invokes all nine and validates eight successful outputs plus the ninth's `turn_not_found`; rmcp stdio proves real discovery/start/EOF. The Shell actor prompt bridge now has an explicit bounded deadline, removing the unbounded await; provider-backed full stdio matrix/live interrupt still require a real provider fixture and remain open.
- [~] TA103-13 [SWARM] Semantic swarm limits now prove N sessions without a hub plus stable mutation errors in 24 unit and 24 C6 integration tests. Human/product binary swarm with independent real actors, epochs/workspaces and bounded resources remains open.
- [ ] TA103-14 [TD] Record per-mutation RED/GREEN and update examples from observed product responses.
